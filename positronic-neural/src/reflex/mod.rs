/// The Reflex Engine (Small Language Model host)
/// Eventually this will wrap `ort` (ONNX Runtime) to run a quantized model locally.
pub struct ReflexEngine;

impl ReflexEngine {
    pub fn new() -> Self {
        Self
    }
}
